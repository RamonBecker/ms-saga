package com.payment.service.infrastructure.shared.constants;

import lombok.Getter;

@Getter

public enum ValueCalculation {

    REDUCE_SUM(0.0),
    MIN_AMOUNT(0.1);

    private final double value;

    ValueCalculation(double value) {
        this.value = value;
    }
}
